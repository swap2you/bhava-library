# Phase 05 — Views and Review Queue

## Delivered

- `curation/views.py` — HTML/CSV/JSON/MD under `data/views/by-*/` (paths as text only)
- `curation/review.py` — `classification_review_queue.csv` for low-confidence / unknown / needs_review

## Tests

- `test_views_no_copy.py` — no binary copies into views

## Gate

- View artifacts are text formats only
