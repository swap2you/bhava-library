# Phase 04 — Deterministic Classification

## Delivered

- `curation/classify.py` — rule-based multi-dimensional labels with confidence and evidence
- Uses `RULE_VERSION` from `taxonomy_seed`
- Writes `resource_classifications` and `classification_evidence`

## Tests

- `test_names_and_classify.py` — coloring-page + sunday-school hits

## Gate

- No originals mutation (`test_no_original_mutation.py`)
