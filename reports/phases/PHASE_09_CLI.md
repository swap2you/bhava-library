# Phase 09 — CLI and bhava.ps1

## Delivered

- `curate` typer subgroup: snapshot, enrich, classify, build-views, review-report, integrity, sunday-school, candidates
- Top-level: `archive-pack`, `archive-restore-check`
- Updated `bhava.ps1` help text

## Gate

- `uv run bhava-lib --help` lists new commands
