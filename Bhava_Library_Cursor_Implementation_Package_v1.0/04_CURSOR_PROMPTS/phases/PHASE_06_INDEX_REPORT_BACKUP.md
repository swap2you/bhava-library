# Phase 06 — Index, Reports, and Backup

Build FTS5 metadata search, Markdown/JSON/CSV reports, non-destructive timestamped backup, checksum validation, and sampled restore verification.

The database must be rebuildable from manifests.
