# Phase 04 — Download Engine

Implement persistent queues, range resume, streaming, disk checks, atomic finalization, bounded retry, path safety, Ctrl+C recovery, and status reporting.

Prove behavior with local HTTP test servers and pytest-httpx fixtures before live use.
