# Phase 00 — Repository Bootstrap

Create the repository structure, Git safety controls, uv project, Python compatibility, logging, configuration, PowerShell entry point, database migration framework, and initial tests.

Prove that `data/**` and common binary extensions cannot be committed.

Deliver a bootstrap report and proceed directly to Phase 01 when green.
