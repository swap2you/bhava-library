# Phase 09 — Real Core Acquisition

Run doctor, live scan, resolution, estimate, and `core` acquisition.

Do not download audio/video.

Automatically download the first safe core batch, verify it, index it, report it, and leave exact resume commands for pending batches.

Preserve state for every stop condition.
