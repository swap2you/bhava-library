# Phase 02 — Database and Manifests

Implement SQLite migrations, domain models, state transitions, JSONL snapshots, CSV exports, audit events, and database rebuild from manifests.

Enable foreign keys, integrity checks, WAL, and FTS5 capability detection.

Proceed after round-trip and migration tests pass.
