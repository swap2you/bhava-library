# Phase 08 — Optional Local Lookup UI

Only after the core acquisition milestone is green, implement or scaffold a localhost-only FastAPI/Jinja/HTMX search screen.

No public binding, CDN, analytics, binary static exposure, or write access to originals.
